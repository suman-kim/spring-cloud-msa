# config client bootstrap.yml
spring:
  cloud:
    config:
      uri: http://localhost:8888  # Config Server의 주소
      name: ecommerce-service  # Config Server에서 가져올 설정 파일의 이름
  profiles:
    active: local